# Archived Socket.IO 9 Hz Runbooks

These goals, handoffs, and runbooks describe the early OnRobot Web /
Socket.IO polling path. That path was measured at about 9 Hz and is now
historical only.

Use current TCP DAQ, RTDE register export, UDP high-speed, or UR RTDE reports
instead for active work.
