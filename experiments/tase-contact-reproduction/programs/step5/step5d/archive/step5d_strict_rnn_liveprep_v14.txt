Step5d strict RNN live-prep TP package

Open on Teach Pendant after controller read-back is verified:
  /programs/andyl/kunwei/step5/step5d/archive/step5d_strict_rnn_liveprep_v14.urp

Version:
  2026-06-15T2310HKT_STEP5D_STRICT_RNN_LIVEPREP_V14

Boundary:
  Contact-capable v14 live-prep package; not a completed reproduction claim.
  Reuses the Step5b contact-search/latch/25.3 scaffold.
  If first-contact orientation error is <= 0.069813 rad,
  it skips the 20 mm lift and 25.2 attitude correction.
  Otherwise it keeps the original lift + 25.2 attitude correction path.
  Stage 24.3/24.4 second contact search is slow-only: near_start_depth is
  0.000 m, max_down is 0.035 m,
  and both far/near speeds are 2.5 mm/s.
  Stage 25.3 is deadband contact acquire: bridge filters normal_load and
  commands only locked-normal Cartesian vx/vy/vz in registers 37..39. It may
  actively recover while raw normal_load is between 0.0 N and 40.0 N,
  with force_norm <= 25.0 N.
  TP enters 25.0 only after filtered normal_load is between 2.0 N
  and 15.0 N, with force_norm <= 25.0 N,
  and bridge cmd_valid is true for 0.150 s.
  Stage 25.0 is different from Step5b: it consumes 37..42 as qd0..qd5 rad/s
  and executes speedj, not Cartesian speedl. v14 bridge must keep cmd_valid=1
  with zero qdot during benign low-load hold and first-sample actual speed dwell,
  freeze path_time_s, and set stop_request with zero qdot if predicted TCP speed
  exceeds the contact-safety backstop immediately, actual TCP speed exceeds the
  backstop for a 0.004 s actual speed dwell, low-load hold exceeds 0.300 s, or
  high-window dwell exceeds 0.030 s.

Bridge profile:
  --step4e-version step5d_strict_rnn_liveprep_v14 --step4e-path-shape cycloid
  --target-force-n 5.0
  --step4e-normal-follow-mode filtered_live
  --step4e-normal-filter-alpha 0.35
  --step4e-normal-min-force-n 2.0

Safety:
  qdot cap: 0.050 rad/s
  speedj acceleration: 0.050 rad/s^2
  Raw normal guard: 50 N. Force norm guard: 60 N. Torque guard: 3.0 Nm.
  50 N is the raw-normal hard guard; force norm hard guard is 60 N.
  No UR zero_ftsensor(), no Kunwei tare/zero/config, no TCP/payload write.
  This package is not a bridge-start or TP-Play authorization.

Reference:
  STEP5_FLOW.md
  config/step5_stage_table.json stage step5d_strict_rnn_liveprep_v14
  UR_FORCE_FRAME_CONTRACT.md
  config/step5d_liveprep_solver_gate.json
