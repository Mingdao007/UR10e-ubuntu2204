Step5d strict RNN ablation v34 12N diagnostic TP package

Open on Teach Pendant after controller read-back is verified:
  /programs/andyl/kunwei/step5/step5d/step5d_strict_rnn_ablation_v34.urp

Version:
  2026-07-15T0054HKT_STEP5D_STRICT_RNN_ABLATION_V34_LATE_FIFO_ACCEL_0P1

Boundary:
  Contact-capable v34 ablation package; not a completed reproduction claim.
  Reuses the Step5b v3 contact-search/latch/25.3 scaffold.
  v34 scaffold delta: Step5b v3 timing/search/scaffold is retained, and only
  Stage25.0 command consumption changes to the layout-tagged Step5d ablation source.
  If TP Play is pressed before the Ubuntu bridge is started, Stage 20 waits up
  to 60.0 s for a fresh bridge heartbeat before timing out.
  Stage 22 entry and Stage 24 far/near search use the shared pre-contact pose
  contract pre_contact_search_gravity_down_v1: TCP +Z targets base -Z with rotvec
  [3.141592654, 0.000000000, 0.000000000].
  After first-contact latch, it does not run the 20 mm lift, 25.2 attitude
  correction, or 24.3/24.4 second contact search.
  no lift and no second contact search remain deliberate requirements.
  Stage 25.3 is a permissive structural handoff. Force-window and Cartesian
  entry-speed values are logged for diagnosis but cannot hold or stop v31.
  v34 Stage 25.3 accepts a bridge-time preload parameter channel when register
  47 equals 521.0: registers 40/41 are filtered
  min/max, 42 is force_norm max, 44 is hold time, and 46 is timeout.
  TP enters 25.0 after a structurally valid cmd_valid handoff; the 5-22 N
  force window is post-run acceptance evidence only, not a live guard.
  Before Stage 25.0, Stage 25.95 requires the bridge to clear registers 37..47
  with cmd_valid=0 and registers 37..42 near zero (<= 0.000500) so stale 25.3 preload parameters or old Stage25 commands cannot be interpreted as current command.
  Stage 25.0 accepts only layout 524.0: registers 37..42 are
  qd0..qd5 for TP speedj. Cartesian and DLS commands are shadow-only.
  Stage25.0 default bridge mode: STEP5D_STAGE25_CONTROL_MODE=speedj_rnn_live.
  Stage25.0 cadence/command-consumption instrumentation: output register 47
  is 1 only when the TP loop accepts a current Stage25 command packet and
  reaches speedl/speedj; bridge CSV records echo tag, cmd_valid, command norm,
  row gap, and loop recv/compute/send/csv timing.
  For v31, after the first accepted command a repeated heartbeat reuses only
  the last guard-approved command for at most 1.000 s;
  before the first accepted command TP only syncs and sends no speed command. The bridge records
  every replay and a longer stale interval remains a stop.
  Bridge control modes:
    STEP5D_STAGE25_CONTROL_MODE=speedl_cartesian_oracle is shadow/diagnostic only for v34 and cannot be selected as a runtime fallback; the only live command mode is speedj_rnn_live, whose Stage25 joint packet carries internal wire marker 524.
    STEP5D_STAGE25_CONTROL_MODE=speedj_dls_oracle computes DLS shadow diagnostics only; v31 forbids sending DLS qdot to TP and forbids DLS runtime fallback.
    STEP5D_STAGE25_CONTROL_MODE=speedj_rnn_live sends strict RNN qdot to speedj.
  v34 is an inactive strict RNN candidate:
  Stage25.0 is not authorized now; after delivery/readback, frozen fingerprint, Review v3, and explicit authorization,
  only speedj_rnn_live may be sent to TP; marker 524 is internal to the Stage25 joint packet encoder. speedl and DLS remain shadow-only.
  Hard stops are structural/frame failure, layout/cmd validity, qdot/heartbeat,
  Dashboard mismatch, timeout, and gross 60 N raw-normal / 100 N force-norm /
  3 Nm torque. Ordinary force, Cartesian/normal speed and displacement, DLS,
  residual, active-bound, and normal-direction checks are diagnostic-only.
  Stage 25.0 diagnostic progress target is 60 s.
  Stage 22 entry movel is 0.060 m/s at 0.090 m/s^2; Stage 24 far search is
  0.0225 m/s down, near search remains 0.0025 m/s.

Bridge profile:
  --step4e-version step5d_strict_rnn_ablation_v34 --step4e-path-shape cycloid
  --target-force-n 12.0
  --step4e-normal-follow-mode filtered_live
  --step4e-normal-filter-alpha 0.55
  --step4e-normal-min-force-n 2.0

Safety:
  Cartesian/tangential/normal speed and displacement: diagnostic-only, no hard cap.
  qdot cap: 0.500 rad/s
  speedj acceleration: 0.100 rad/s^2
  Raw normal guard: 60 N. Force norm guard: 100 N. Torque guard: 3.0 Nm.
  60 N raw-normal/100 N force-norm and 3.0 Nm torque are sensor hard guards only;
  human safety still depends on the external cage/operator/E-stop boundary.
  No UR zero_ftsensor(), no Kunwei tare/zero/config, no TCP/payload write.
  This package is not a bridge-start or TP-Play authorization.

Reference:
  STEP5_FLOW.md
  config/step5_stage_table.json stage step5d_strict_rnn_ablation_v34
  config/step_pose_contract_table.json contract pre_contact_search_gravity_down_v1
  UR_FORCE_FRAME_CONTRACT.md
  config/step5d_liveprep_solver_gate.json
