Implement one bounded OFFLINE integration diagnostic in this isolated worktree. You are the sole coding writer, Homepool grok-4.6 high. Do not spawn agents, operate hardware/network endpoints, change Git state, loosen gates, edit unrelated paths or start an adviser.

Scope: add tests/test_contact_yield_full_writer.py and tools/measure_yield_full_writer.py (and one small shared offline fixture module if needed). Existing tests/test_contact_yield_writer_loop.py currently executes real LiveR004Writer.execute_attempt with stub control; tests/test_contact_yield_provider.py executes native YieldContactProvider + CanonicalQualificationControl separately. Compose the actual native provider and actual CanonicalQualificationControl.step inside the actual mature execute_attempt loop, including real packet history, evidence collector and path_sample_sink. In-memory transport and deterministic observation clock are allowed. Baseline-success preparation/admission may be explicitly fixture-provided; DO NOT substitute the native PATH law/QP, qualification step, publication/evidence path or claim real transport admission.

First inspect report/yield-execution-seam-v1/README.md, report/yield-latency-trace-v1/README.md, tests/test_contact_yield_writer_loop.py, test_contact_yield_provider.py, test_contact_qualification_provider.py, tools/yield_contact_provider.py and actual writer/qualification interfaces. Use calibrated home/pose and consistent observations; stationary observation fixture is acceptable to isolate composition, clearly not task-performance evidence. Do not fabricate a moving TCP from reference while leaving q fixed or claim stationary fixture completed the physical task. A legitimate task-quality rejection may be retained while software lifecycle is verified. No production code edit is authorized in this bounded assignment: report a concrete defect to main with reproducible failure if discovered.

Diagnostic must run all SFC, DSFC and MSFC g50 (parameters from report/yield-frozen-transfer-v1/protocol.json, never original MSFC seed). Preserve 1 s entry + full 62.831853 s PATH; assert actual provider instance/state carries across boundary, emitted packet qdot comes from real native step, consumed sequence binds phase/time and full formal evidence is retained. Keep freshness/identity/cap semantics intact; clocks are simulated receive clocks, not real latency. Runtime deadline=None permitted only explicit offline fixture so timed outliers are recorded rather than hidden; unchanged production deadline remains 2 ms.

Provide a CLI callable in a fresh process with --output NEW_DIRECTORY, retaining per-tick whole-loop wall and thread CPU durations, native/core duration if feasible, packet/evidence counts, phase coverage, exact method parameters/runtime identity, all failures, >2ms counts, existing GC policy and scope limitations. Measure real CPU/wall despite fake logical clock; exclude intentional synthetic sleep but state exact measurement boundary. Do not call this formal timing qualification or IO latency. Use existing writer GC policy, no independent production change. Keep native states and shared qp observed unchanged, no discarded warmup/outliers. Add focused tests on combined command path and stale/fault behavior as appropriate.

Managed Python is /home/andy/.codex-worktrees/contact-yield-recovery-20260920/experiments/tase-contact-reproduction/.venv-contact-six/bin/python. Run from this experiment root with env -u PYTHONHOME -u VIRTUAL_ENV PYTHONPATH=/opt/ros/humble/lib/python3.10/site-packages:/opt/ros/humble/local/lib/python3.10/dist-packages PYTHONNOUSERSITE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1. Main will review/run full diagnostic; you should run meaningful focused checks. Do not add approval/checksum frameworks. Preserve raw failure evidence in a new runs/yield-full-writer-development directory if needed. Report exact tests and limitations, do not commit.
