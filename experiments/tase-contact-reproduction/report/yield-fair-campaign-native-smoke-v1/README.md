# Native full-period engineering pair

Two native SFC runs, each 62.83185307179586 s PATH plus 1 s entry, 2 ms controller and 8 plant substeps. Both artifacts pass the real full-state evaluator; pair is valid and meets the declared development bands. J = 77.4531942534, recovery 3.652 s relative to matched nominal. This tests integration, not comparative benefit.

Artifacts are diagnostic_seed with complete states, retained locally with hashes in result.json. No training ledger exists for this check; zero formal or validation units. No hardware was accessed. Exact full-state forward replay is not claimed by this schema/evaluation check.
