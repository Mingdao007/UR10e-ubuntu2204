# Retained initial analysis check

The trials completed normally. The first analysis correctly found that the
parameter-derived law_identity differs when minimum_metric_eigenvalue changes,
but mistakenly treated that intended derived difference as an invariant failure.
The repaired analysis reconstructs and verifies each native law/snapshot binding
before excluding those derived identity fields from the otherwise strict match.
See ../yield-frozen-memory-v1 for the verified report. These outputs are retained
as the original failed analysis attempt, not as accepted matching evidence.
