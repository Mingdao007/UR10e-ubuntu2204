# Main integration

Accepted repaired Homepool Grok delivery after 17 independently passing tests and a complete read-only report over the real running campaign snapshot. Both initial counterexamples are retained. The same native session was resumed; model grok-4.6, Homepool endpoint, cwd and requested/attested high were verified; effective server effort remains unexposed. No model fallback occurred.

Real-data acceptance: 81 complete artifacts verified against ledger digests; 350 descriptors agree within 1e-12 with prior round-8 recomputation. All completed pair objectives/feasibility flags match ledger evidence; all 72 slots remain present. SFC stops after 8 and has 16 stopped slots; no full three-method freeze is asserted. A running unit is shown in slots but not extended into best-so-far curves. Three PNGs were visually inspected.

The report is retained in report/yield-training-report-real-v1. It reflects its recorded snapshot time, not subsequent training. Initial plot hatching marks the relevant member condition (nominal feasibility or disturbed guards), not whole-pair feasibility; the JSON/CSV retain all pair flags. Low-load duration means load below 1 N, not geometric separation. Plots are development diagnostics, not final paper evidence of superiority or physical qualification. The all-zero nominal low-load panel uses Matplotlib autoscaling; no negative measured durations are present.

Only new reporter/test/report files are integrated. Frozen training code, configurations, equations and native binaries remain unchanged; this read-only validation consumes no experiment budget. Plotting uses the existing system Matplotlib through an explicit PYTHONPATH in the reporter process, leaving the running campaign interpreter/environment untouched.

Main follow-up normalized generated CSV record endings to LF after Git whitespace checking flagged the standard csv module CRLF default. Parsed CSV cells were compared before/after and are identical. The writer receipt retains the original repaired-delivery hashes; this one-line formatting change belongs to main integration.
