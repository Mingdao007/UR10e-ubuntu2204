# Validation runner integration

Original independent23tests passed but Main reproduced acceptance of a self-hashed candidate export with no source ledger/campaign identity. Same native Grok session repaired it; repaired26tests independently passed. Original counterexample and both native delivery receipts remain preserved.

Main integrated only the two new validation modules and their tests; no frozen training source was edited. Main added comparison of actual shared training execution identities (including native/QP binaries), training config digest and canonical campaign-protocol digest against the freeze. Final focused verification:27passed, one xacro deprecation warning.

Four actual native diagnostic cases cover SFC_RADIAL and MSFC_IDENTITY, each cold/warm, nonzero7degree prior, old development surface (.8,.4),20ms PATH. The raw executable methods remain radialSFC andMSFC, initial priors and every recorded controller/plant snapshot schema pass. See ../yield-validation-native-smoke-v1/result.json. These are short interface checks, not a full validation cell, closed-loop performance comparison or exact forward replay. No validation/holdout unit was launched.

Production create requires complete24-pair training freeze with all3methods. Tests use explicit synthetic complete ledgers and evaluator fixtures for lifecycle; they do not prove the remaining physical or scientific task complete. Actual frozen-incumbent validation must wait for real training completion and matching source identities.
