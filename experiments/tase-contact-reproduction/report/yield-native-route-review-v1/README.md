# Main review of draft native-route binding

Read-only constructor check against the in-progress isolated implementation,
bound by source hash in anchor-binding.json. This is not the writer's final
accepted delivery. No controller tick, command publication or endpoint access.

Two genuine SFC runtimes use the same reviewed model, native law, QP and NO-v3
observer, but their task anchors differ by 1 mm. Their runtime identities differ;
their controller identities are equal. The draft binding for runtime A accepts
runtime B because require_runtime checks controller identity but omits the
complete runtime identity. The existing runtime identity already binds anchor,
basis, calibrated model and endpoint policy, so the admission binding should
carry and verify it before a provider is prepared. No new identity mechanism
or permission flow is required.

Reproduce from the isolated experiment root using its tools on PYTHONPATH and
the primary .venv-contact-six interpreter with existing ROS Python paths.
The script uses the retained home receipt and existing offline native builds.
The named model and runtime are not fresh physical qualification evidence.
