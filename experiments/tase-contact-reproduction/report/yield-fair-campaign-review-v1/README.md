# Main read-only draft review

The isolated Grok writer remains active. These are counterexamples against exact draft source hashes, not a judgement on its final delivery. Main did not edit the writer scope.

1. `_require_fullstate` accepts 31,916 records whose controller_snapshot and simulator_snapshot are both null. This proves the helper's snapshot-content check is insufficient; it does not by itself prove the entire pair evaluator accepts a fabricated artifact.
2. A temporary YieldContactLedger with a complete nominal row carrying nominal_feasible=false and a complete disturbed row carrying pair_feasible=true but no disturbed_guards_ok returns a training observation marked feasible. The caller assertion overrides actual nominal rejection. A later freeze failure would not repair contaminated EI selection.

Both probes had stable source hashes during execution. No native campaign, robot operation, training unit or holdout was executed. Final delivery must be rechecked because the writer can still change these paths. Repair requires real full-state validation and an explicit conjunction of independently validated nominal and disturbed gates, retaining legacy behavior only where the old contract is genuinely in use.
