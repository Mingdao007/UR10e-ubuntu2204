# Main acceptance checkpoint

Initial ten fake-runner tests passed but the public status command accepted mutation of manifest slot0 gain to .199. Reproduction: /tmp/yield-mechanism-main-counterexample-9b6nxr64, zero executed trials. Same Grok session repaired bindings and outer settings dispatch. Main reran fifteen tests: all passed in0.46s. A fresh create/status using production libraries succeeded; the same gain mutation was rejected as manifest identity drift (temporary evidence /tmp/yield-mechanism-repaired-main-xny_akne).

Five actual native arms were exercised with generated runner kwargs,20ms diagnostic PATH plus1s entry. Actual methods/complete parameter dictionaries matched; each produced510 full records and10 PATH rows, no native failure. This is an interface probe, not full-period or performance acceptance. No scheduled60 diagnostic trials or reserved validation were executed in this probe.

Both lane receipts bind the same session01a0bd6e-662a-7312-bd1d-c7838c48dcc2, requested grok-4.6/high and Homepool endpoint. Native-reported model is grok-4.6; effort is launch/config attestation, not independent server evidence. No fallback.

Full-period execution/sealing remains the next required check. Existing training evidence and validation prerequisites are unchanged.
