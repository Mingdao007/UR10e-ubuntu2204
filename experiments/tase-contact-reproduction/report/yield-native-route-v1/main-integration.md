# Main integration review

Native Grok session 01a0bc90-5ed9-7252-8bf4-6e90283d70ce completed through hp-grok / grok-4.6, Homepool endpoint, high requested/attested at launch; no fallback. Provider does not expose an independent effective-effort field. Original delivery hashes and route receipt retained.

Main fixed the demonstrated cross-anchor binding counterexample from ../yield-native-route-review-v1. Binding now checks and exposes the complete runtime identity, including anchor, basis and periodic continuation policy. Tests reject cross-anchor and cross-basis provider construction. Also enforce the historical model receipt file digest, previously only format-checked. This remains historical reconciliation, not fresh hardware evidence.

Independent tests: 42 passed including the full-cycle provider fixture. After receipt integrity repair, 27 relevant constructor/injection tests passed. The initial misspelled test path caused no tests to run; that log is retained separately. Only the existing xacro deprecation warning remains.

Unmocked native prepare_control now works for all three methods while preserving legacy contract routing, exact-instance/package guards, one-time use, real deadlines, and persistent state. No endpoint or robot operation occurred. Formal training and holdout budgets remain unused.
