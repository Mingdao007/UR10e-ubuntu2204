# UR5e EOAT model workspace

这个目录用于保存 `UR5e`（Universal Robots 5e）末端执行器 `EOAT`（end-effector, 末端执行器）建模输出。

## 目录规范

- 每个版本单独放在一个子文件夹内，例如 `v1/`、`v2/`、`v3/`
- `v1`、`v2` 是早期分层结构的历史版本
- 从 `v3` 起，每个版本目录内部默认直接平铺文件，不再继续套 `source / parts / assembly / preview / notes`
- 新版本从 `v5/` 开始继续使用这个新主路径
- 保留所有旧版本
- 不覆盖旧版本
- 不把不同版本文件混放在同一级目录

## 当前验证顺序

1. 拆下 `RG2`，目标安装对象锁定为 `UR5e` 裸法兰
2. 获取裸法兰照片：
   - 正视图
   - 轻微侧视图
   - 普通尺子同框
3. 根据照片静态复核当前版本的机器人侧接口
4. 若静态复核通过，先打印对应版本的 `fit-check`
5. `fit-check` 通过后，再考虑打印 `base` 或 `onepiece`

## 当前默认规则

- `UR5e` 裸法兰安装风险未解除前，第一打印件必须是 `fit-check`
- 分件版和整体版默认都保留
- 每个版本都应附带假设说明、未确认尺寸、下一步建议测量项

## 当前已存在版本

- `v1`：基础法兰转接盘
- `v2`：探针式接触头概念版，已包含 `fit-check`
- `v3`：roller probe reference
- `v4`：flat-pad / fit-check baseline，80 mm adapter OD，已有 May 7 实物 fit-check 照片和视频
- `v5_receiver_fitcheck`：M6 receiver/thread coupon，不是完整 EOAT 版本
- `v6_slim_adapter_72mm`：v4 的 slim adapter 继任版，72 mm adapter OD，保留 4 x M6 on 50 mm PCD 和中心 register/recess
- `v7_ksm8n_ball_transfer_tool`：长版完整被动 rolling-contact EOAT，使用 KSM-8N M6 ball transfer，TCP 距法兰面 140 mm，采用 captive M6 nut receiver
- `v8_ksm8n_ball_transfer_tool_85mm`：KSM-8N 短版完整 EOAT，TCP 距法兰面 85 mm，采用 side nut slot + captive M6 nut receiver，receiver head 为 32 mm OD
- `v9_ksm8n_receiver_slim_85mm`：在 v8 基础上只把最后 receiver head 从 32 mm OD 缩到 28 mm OD，TCP、法兰、base/taper/neck 和 captive M6 nut receiver 尺寸逻辑保持不变
- `v10_ksm8n_receiver_recessed_face_85mm`：在 v9 基础上给 receiver front face 增加 24.5 mm OD、1.8 mm deep 的 annular recess，保留 18.0 mm central seating boss；assembly/onepiece 只包含打印 body，不包含购买的 KSM-8N 金属件
- `v11_ksm8n_receiver_flat_recess_85mm`：当前第一版打印候选，把 v10 的 annular recess 改为 flat-bottom recessed front face；中间保留 18.0 mm central seating boss，boss 外侧整片前端面下凹 1.5 mm 并切到 receiver 外缘
